name: ShadowMask Modular Build

on:
  push:
    branches: [master]
    paths:
      - "app/**"
      - "native/**"
      - "build.py"
      - ".github/workflows/build.yml"
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  # ==========================================
  # 1. Build Native (C/C++) binaries for all ABIs
  # ==========================================
  build-native:
    name: Build Native Binaries (NDK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: "recursive"

      - uses: ./.github/actions/setup
        with:
          is-asset-build: true

      - name: Compile Native C/C++ Binaries
        run: ./build.py native

      - name: Verify Output Directory
        run: |
          echo "Checking contents of native/out/ directory:"
          ls -R native/out/

      - name: Upload Native Binaries Artifact
        uses: actions/upload-artifact@v4
        with:
          name: native-binaries
          path: native/out/
          retention-days: 1
          if-no-files-found: error

  # ==========================================
  # 2. Build Debug APKs (classic + ng) in parallel with release
  # ==========================================
  build-debug-apk:
    name: Build Debug APKs
    needs: build-native
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup
        with:
          is-asset-build: true

      - name: Download Native Binaries
        uses: actions/download-artifact@v4
        with:
          name: native-binaries
          path: native/out/

      - name: Build Debug APK (classic)
        run: ./build.py app

      - name: Build Debug APK (ng)
        run: ./build.py app-ng

      - name: Rename Debug APKs
        run: |
          mv out/app-debug.apk    out/shadowmask-debug.apk
          mv out/app-ng-debug.apk out/shadowmask-ng-debug.apk

      - name: Upload Debug APK (classic)
        uses: actions/upload-artifact@v7
        with:
          name: shadowmask-debug
          path: out/shadowmask-debug.apk
          compression-level: 9

      - name: Upload Debug APK (ng)
        uses: actions/upload-artifact@v7
        with:
          name: shadowmask-ng-debug
          path: out/shadowmask-ng-debug.apk
          compression-level: 9

  # ==========================================
  # 3. Build Signed Release APKs (classic + ng) & Publish
  # ==========================================
  build-release-apk:
    name: Build Signed Release APKs & Publish
    needs: build-native
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup
        with:
          is-asset-build: true

      - name: Download Native Binaries
        uses: actions/download-artifact@v4
        with:
          name: native-binaries
          path: native/out/

      - name: Configure Keystore & Signing
        if: github.event_name != 'pull_request'
        env:
          GH_PAT: ${{ secrets.GITHUB_PERSONAL_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          SIGNING_DIR=".github/signing"
          KEYSTORE_B64="$SIGNING_DIR/keystore.jks.b64"
          SIGNING_ENV_FILE="$SIGNING_DIR/signing.env"

          if [ -f "$KEYSTORE_B64" ] && [ -f "$SIGNING_ENV_FILE" ]; then
            source "$SIGNING_ENV_FILE"
          else
            STORE_PASSWORD="shadowmask-$(openssl rand -hex 12)"
            KEY_PASSWORD="shadowmask-$(openssl rand -hex 12)"
            KEY_ALIAS="shadowmask"

            keytool -genkeypair \
              -keystore shadowmask-release.jks \
              -alias "$KEY_ALIAS" \
              -keyalg RSA \
              -keysize 4096 \
              -validity 10000 \
              -storepass "$STORE_PASSWORD" \
              -keypass "$KEY_PASSWORD" \
              -dname "CN=ShadowMask Build, OU=CI, O=ShadowMask, L=Unknown, ST=Unknown, C=US" \
              -noprompt

            mkdir -p "$SIGNING_DIR"
            openssl base64 -A -in shadowmask-release.jks > "$KEYSTORE_B64"
            printf 'STORE_PASSWORD=%s\nKEY_PASSWORD=%s\nKEY_ALIAS=%s\n' \
              "$STORE_PASSWORD" "$KEY_PASSWORD" "$KEY_ALIAS" > "$SIGNING_ENV_FILE"

            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git remote set-url origin "https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}.git"
            git add "$KEYSTORE_B64" "$SIGNING_ENV_FILE"
            git commit -m "ci: add generated release keystore [skip ci]"
            git push origin HEAD:master
          fi

          openssl base64 -d -A -in "$KEYSTORE_B64" > shadowmask-release.jks
          chmod 600 shadowmask-release.jks

          printf 'keyStore=%s\nkeyStorePass=%s\nkeyAlias=%s\nkeyPass=%s\n' \
            "$(pwd)/shadowmask-release.jks" "$STORE_PASSWORD" "$KEY_ALIAS" "$KEY_PASSWORD" \
            > config.prop

      - name: Build Release APK (classic)
        run: ./build.py app -r

      - name: Build Release APK (ng)
        run: ./build.py app-ng -r

      - name: Rename Release APKs
        run: |
          mv out/app-release.apk    out/shadowmask-release.apk
          mv out/app-ng-release.apk out/shadowmask-ng-release.apk

      - name: Upload Release APK (classic)
        uses: actions/upload-artifact@v7
        with:
          name: shadowmask-release
          path: out/shadowmask-release.apk
          compression-level: 9

      - name: Upload Release APK (ng)
        uses: actions/upload-artifact@v7
        with:
          name: shadowmask-ng-release
          path: out/shadowmask-ng-release.apk
          compression-level: 9

      - name: Publish to GitHub Releases
        if: github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete latest --yes 2>/dev/null || true
          git tag -d latest 2>/dev/null || true
          git push origin :refs/tags/latest 2>/dev/null || true
          gh release create latest \
            --title "ShadowMask Build (latest)" \
            --notes "Automatically built from the master branch. Install shadowmask-ng-release.apk (new Compose UI) or shadowmask-release.apk (classic UI)." \
            out/shadowmask-ng-release.apk \
            out/shadowmask-release.apk
